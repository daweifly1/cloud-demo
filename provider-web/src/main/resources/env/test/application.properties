server.port=@server.port@
spring.application.name=provider-web

#java -jar 打包之后的SpringBoot.jar  --server.port=8000
#默认的注册域
eureka.client.serviceUrl.defaultZone=@defaultZone@

#控制台彩色输出
spring.output.ansi.enabled=ALWAYS